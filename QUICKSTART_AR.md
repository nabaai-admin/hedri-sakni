# نظام إدارة حجوزات الأراضي - دليل البدء السريع

## 📌 نظرة عامة

تم إنشاء نظام متكامل لإدارة ومراجعة محاولات حجز الأراضي مع التكامل مع UiPath.

## 🎯 المميزات الرئيسية

### الواجهة الخلفية (Backend)
- ✅ Flask REST API مع PostgreSQL
- ✅ مصادقة JWT آمنة
- ✅ توثيق Swagger/OpenAPI كامل
- ✅ جدولة المهام الخلفية (APScheduler)
- ✅ تكامل مع UiPath API
- ✅ سجلات كاملة وقابلة للتتبع

### الواجهة الأمامية (Frontend)
- ✅ React.js مع دعم RTL للعربية
- ✅ تصميم عصري ومتجاوب
- ✅ واجهة مستخدم متميزة مع رسوم متحركة
- ✅ إدارة البيانات مع React Query

## 🚀 البدء السريع

### 1. التثبيت التلقائي
```bash
chmod +x setup.sh
./setup.sh
```

### 2. التثبيت اليدوي

#### الواجهة الخلفية
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# إنشاء قاعدة البيانات
createdb hedri_sakni

# نسخ ملف الإعدادات
cp .env.example .env
# قم بتعديل .env بإعداداتك
```

#### الواجهة الأمامية
```bash
cd frontend
npm install
```

### 3. تشغيل التطبيق

#### تشغيل الواجهة الخلفية
```bash
cd backend
source venv/bin/activate
python run.py
```
سيعمل على: http://localhost:5000

#### تشغيل الواجهة الأمامية
```bash
cd frontend
npm run dev
```
سيعمل على: http://localhost:5173

## 🔑 بيانات الدخول الافتراضية

**اسم المستخدم:** admin  
**كلمة المرور:** admin123

⚠️ **يجب تغييرها في بيئة الإنتاج!**

## 📚 الوصول إلى التوثيق

بعد تشغيل الواجهة الخلفية:
- **Swagger UI:** http://localhost:5000/api/docs
- **API Spec:** http://localhost:5000/apispec.json

## 🗂️ هيكل المشروع

```
hedri-sakni/
├── backend/                 # Flask REST API
│   ├── app/
│   │   ├── routes/         # نقاط النهاية API
│   │   ├── services/       # منطق الأعمال
│   │   ├── models.py       # نماذج قاعدة البيانات
│   │   └── config.py       # الإعدادات
│   ├── requirements.txt
│   └── run.py
├── frontend/               # React Application
│   ├── src/
│   │   ├── pages/         # صفحات التطبيق
│   │   ├── components/    # المكونات
│   │   └── services/      # عميل API
│   └── package.json
└── README.md
```

## 📋 الوظائف الأساسية

### 1. لوحة التحكم
- إحصائيات عامة
- معدل النجاح
- تحليلات حسب المنطقة

### 2. إدارة البيانات
- **المناطق:** إضافة، تعديل، حذف
- **العملاء:** إدارة كاملة مع الحقول العربية
  - الاسم
  - رقم الهاتف
  - الرقم الوطني
  - المنطقة
  - حالة الحجز (OPEN/SUCCESS/FAILED)
- **مواعيد الحجز:** جدولة وإدارة المواعيد

### 3. التقارير والتحليلات
- إحصائيات مجمعة حسب المنطقة
- فلترة حسب التاريخ والحالة
- معدلات النجاح
- سجل تفصيلي للمحاولات

### 4. التكامل التلقائي
- إرسال طلبات تلقائية لـ UiPath في المواعيد المحددة
- استقبال تحديثات الحالة عبر Webhook
- تخزين الرسائل كما وردت بالضبط

## 🔧 الإعدادات المطلوبة

### ملف `.env` للواجهة الخلفية
```env
DATABASE_URL=postgresql://user:password@localhost:5432/hedri_sakni
UIPATH_API_URL=https://your-uipath-instance.com/api
UIPATH_API_KEY=your-api-key
UIPATH_CLIENT_ID=your-client-id
UIPATH_CLIENT_SECRET=your-client-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password
```

## 🌐 نقاط النهاية الرئيسية

### المصادقة
- `POST /api/auth/login` - تسجيل الدخول

### المناطق
- `GET /api/areas` - قائمة المناطق
- `POST /api/areas` - إضافة منطقة
- `PUT /api/areas/:id` - تحديث منطقة
- `DELETE /api/areas/:id` - حذف منطقة

### العملاء
- `GET /api/customers` - قائمة العملاء
- `POST /api/customers` - إضافة عميل
- `PUT /api/customers/:id` - تحديث عميل
- `DELETE /api/customers/:id` - حذف عميل

### مواعيد الحجز
- `GET /api/reservations` - قائمة المواعيد
- `POST /api/reservations` - إضافة وجدولة موعد
- `PUT /api/reservations/:id` - تحديث موعد
- `DELETE /api/reservations/:id` - حذف موعد

### التحليلات
- `GET /api/analytics/summary` - إحصائيات مجمعة
- `GET /api/analytics/attempts` - سجل المحاولات التفصيلي

### التكامل الخارجي
- `POST /api/external/update` - Webhook لتحديث الحالة من UiPath
- `GET /api/external/health` - فحص الصحة

## 🎨 مميزات الواجهة

- تصميم عصري ومتميز
- رسوم متحركة سلسة
- دعم كامل للغة العربية (RTL)
- متجاوب مع جميع الأجهزة
- رسوم بيانية تفاعلية
- إشعارات Toast

## 🔄 كيفية عمل الجدولة التلقائية

1. عند إنشاء موعد حجز، يتم جدولته تلقائياً
2. في الوقت المحدد، يرسل النظام طلبات لجميع العملاء في المنطقة بحالة OPEN
3. يتم تسجيل جميع الطلبات والاستجابات
4. ينتظر النظام تحديث الحالة النهائية عبر Webhook

## 📊 قاعدة البيانات

### الجداول
- **areas** - المناطق الجغرافية
- **customers** - معلومات العملاء
- **reservation_slots** - مواعيد الحجز المجدولة
- **reservation_attempts** - سجل كامل للمحاولات

## 🐛 حل المشاكل الشائعة

### مشكلة الاتصال بقاعدة البيانات
```bash
# تحقق من تشغيل PostgreSQL
sudo systemctl status postgresql

# إنشاء قاعدة البيانات
createdb hedri_sakni
```

### المنفذ مستخدم
```bash
# الواجهة الخلفية (منفذ 5000)
lsof -ti:5000 | xargs kill -9

# الواجهة الأمامية (منفذ 5173)
lsof -ti:5173 | xargs kill -9
```

## 📝 ملاحظات مهمة

1. **الأمان:** غيّر بيانات الدخول الافتراضية في الإنتاج
2. **UiPath:** تأكد من تكوين بيانات اعتماد UiPath الصحيحة
3. **قاعدة البيانات:** استخدم كلمة مرور قوية لقاعدة البيانات
4. **CORS:** حدّث CORS_ORIGINS في الإنتاج

## 🚀 النشر للإنتاج

### الواجهة الخلفية
```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 run:app
```

### الواجهة الأمامية
```bash
npm run build
# انسخ محتويات dist/ إلى خادم الويب
```

## 📞 الدعم

للحصول على الدعم، يرجى التواصل مع فريق التطوير.

---

تم البناء بـ ❤️ باستخدام Flask و React و PostgreSQL
